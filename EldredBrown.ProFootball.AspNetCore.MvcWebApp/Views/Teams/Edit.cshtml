@model EldredBrown.ProFootball.AspNetCore.MvcWebApp.ViewModels.Teams.TeamsDetailsViewModel

@{
    ViewData["Title"] = "Edit Team";
}

<h1>Team</h1>

<h4>Edit</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div class="text-danger" asp-validation-summary="ModelOnly"></div>
            <input type="hidden" asp-for="Team.Id" />
            <div class="form-group">
                <label class="control-label" asp-for="Team.Name"></label>
                <input class="form-control" asp-for="Team.Name" />
                <span class="text-danger" asp-validation-for="Team.Name"></span>
            </div>
            <div class="form-group">
                <button class="btn btn-primary" type="submit">Save</button>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
